一阶段：初始化项目基础架构 + 全局配置

项目 DevEnterpriseAI 基础架构已搭建完成。

## 项目目录结构
```
/home/zt/DevEnterpriseAI/
├── __init__.py              # 项目主入口
├── config/                  # 全局配置模块
│   ├── __init__.py
│   └── settings.py          # 配置管理（LLM、向量库、内存、日志等）
├── core/                    # 核心内核模块
│   ├── __init__.py
│   └── llm.py               # LLM接口封装（支持OpenAI兼容接口）
├── tools/                   # 自定义工具模块
│   ├── __init__.py
│   └── base.py              # 工具基类（注册机制、工厂模式）
├── knowledge/               # 知识库模块
│   ├── __init__.py
│   ├── ingest.py            # 文档入库脚本（支持txt/pdf/docx/md）
│   └── vectorstore/         # 向量库持久化目录
├── utils/                   # 通用工具模块
│   ├── __init__.py
│   ├── logger.py            # 日志工具（轮转日志）
│   └── exception.py         # 异常体系定义
├── logs/                    # 日志输出目录
├── .env                     # 环境变量配置（密钥管理）
├── .env.example             # 环境变量模板
└── requirements.txt         # Python依赖
```
## 核心特性
模块 功能 config 基于Pydantic的强类型配置，.env文件管理，支持模型API密钥、地址、温度、向量库路径等 core EnterpriseLLM 统一封装OpenAI兼容接口，支持私有化大模型； EmbeddingsClient 向量化客户端 tools BaseTool 抽象基类 + ToolFactory 工厂模式，支持工具注册和动态加载 knowledge DocumentLoader 多格式文档加载 + KnowledgeIngestor 分块向量化存储 + Chroma持久化 utils 企业级日志（轮转）+ 层次化异常体系

## 快速开始
1. 复制配置模板并填写实际值：
```
cp .env.example .env
```
2. 安装依赖：
```
pip install -r requirements.txt
```
3. 在 .env 中配置您的私有化LLM地址和API密钥，即可使用框架提供的所有功能。



二阶段：搭建 LLM 全局调用 + 会话记忆模块
底层大模型调用层和记忆管理层已完成开发。

## 新增功能
### 1. 统一LLM调用层 (core/llm.py)
功能 说明 LLMMonitor监控器 记录每次模型调用的耗时、入参、出参等信息 参数验证 自动验证消息格式，防止非法输入 统一异常捕获 网络超时、连接错误、认证失败、API错误等 OpenAI兼容接口 chat_completion() 方法支持标准OpenAI格式 流式输出支持 完整的流式生成功能，带日志记录

### 2. 会话记忆管理层 (core/memory.py)
功能 说明 ConversationMemory 会话记忆管理器，支持自动压缩对话历史 对话摘要 基于ConversationSummaryMemory自动生成摘要 分层记忆 短期详细记忆 + 长期摘要记忆 溢出保护 超过阈值自动压缩，防止上下文溢出 序列化支持 to_dict() / from_dict() 支持状态持久化

### 3. 异常体系 (utils/exception.py)
异常类型 触发场景 LLMError 模型调用失败 NetworkError 网络超时或连接错误 ValidationError 参数验证失败 ConfigurationError 配置错误

## 核心模块结构
```
core/
├── __init__.py
├── llm.py           # 企业级LLM封装（含监控和异常处理）
└── memory.py        # 会话记忆管理（自动摘要压缩）
```
## 使用示例
```
# 创建LLM实例
from core import LLMFactory, MemoryFactory

llm = LLMFactory.create_llm(
    api_key="your-api-key",
    base_url="https://your-private-model/v1",
    model_name="gpt-4"
)

# 创建记忆实例
memory = MemoryFactory.create_summary_memory(llm=llm)

# 添加消息
memory.add_user_message("你好")
memory.add_ai_message("你好！我是企业AI助手。")

# 获取历史
history = memory.get_history()
summary = memory.get_summary()
```
代码已通过语法检查，可直接用于后续Agent开发。